{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "base.schema.json",
  "title": "Base definitions",
  "description": "Auditable base type for all domain entities",
  "$defs": {
    "Auditable": {
      "type": "object",
      "description": "Common fields for all persistent entities",
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid",
          "description": "Unique identifier"
        },
        "created_at": {
          "allOf": [{ "$ref": "dates.schema.json#/$defs/UtcDateTime" }],
          "description": "UTC timestamp when the record was created. Example: '2024-06-15T14:30:00Z'"
        },
        "updated_at": {
          "allOf": [{ "$ref": "dates.schema.json#/$defs/UtcDateTime" }],
          "description": "UTC timestamp when the record was last updated. Example: '2024-06-15T14:30:00Z'"
        }
      },
      "required": ["id", "created_at"]
    }
  }
}
